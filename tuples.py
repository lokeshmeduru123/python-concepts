# tuple is defined it can't be altered, where as list can be altered 
# tuple will not have oprations like append, extend, its an optized one to use in python script consumes less memory  but it suports only count & index

